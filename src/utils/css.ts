import { css as css0, SystemStyleObject } from '@styled-system/css';
export function css(input?: SystemStyleObject): string {
    let i = css0(input);
    let s = i as unknown as string;
    return s;
}
export default css;